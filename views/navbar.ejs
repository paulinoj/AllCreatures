      <!-- Navbar
      ================================================== -->

      <% if (!login) { %>
        <div class="bs-docs-section clearfix margin-after">
          <div class="row">
            <div class="col-lg-12">
              <div class="bs-component">
                <nav class="navbar navbar-default navbar-fixed-top">
                  <div class="container-fluid">
                    <div class="navbar-header">
                      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                      </button>
                      <a class="navbar-brand little_logo_font" href="#">All Creatures</a>
                      
                    </div>

                    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                      <ul class="nav navbar-nav">

                        <% if (page == '/') { %>
                          <li class="active"><a href="#">Home<span class="sr-only">(current)</span></a></li>
                        <% } else { %>
                          <li><a href="/">Home</a></li>
                        <% } %>



                        <% if (page == '/adoptableAnimals') { %>
                          <li class="active"><a href="#">Adoptable Animals<span class="sr-only">(current)</span></a></li>
                        <% } else { %>
                          <li><a href="/adoptableAnimals">Adoptable Animals</a></li>
                        <% } %>


                        <% if (page == '/adoptionApp') { %>
                          <li class="active"><a href="#">Adoption Application<span class="sr-only">(current)</span></a></li>
                        <% } else { %>
                          <li><a href="/adoptionApp">Adoption Application</a></li>
                        <% } %>


                        <% if (page == '/ourClients') { %>
                          <li class="active"><a href="#">Our Clients<span class="sr-only">(current)</span></a></li>
                        <% } else { %>
                          <li><a href="/ourClients">Our Clients</a></li>
                        <% } %>


                        <% if (page == '/donate') { %>
                          <li class="active"><a href="#">Donate<span class="sr-only">(current)</span></a></li>
                        <% } else { %>
                          <li><a href="/donate">Donate</a></li>
                        <% } %>


                        <% if (page == '/generalInfo') { %>
                          <li class="active"><a href="#">General Information<span class="sr-only">(current)</span></a></li>
                        <% } else { %>
                          <li><a href="/generalInfo">General Information</a></li>
                        <% } %>


                      </ul>

                      <ul class="nav navbar-nav navbar-right">
                        <li><a href="/login">Administrator Login</a></li>
                      </ul>
                    </div>
                  </div>
                </nav>
              </div>
            </div>
          </div>
        </div>
      <% } else { %>
        <div class="bs-docs-section clearfix margin-after">
          <div class="row">
            <div class="col-lg-12">
              <div class="bs-component">
                <nav class="navbar navbar-default navbar-fixed-top">
                  <div class="container-fluid">
                    <div class="navbar-header">
                      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                      </button>
                      <a class="navbar-brand little_logo_font" href="#">All Creatures</a>
                      
                    </div>

                    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                      <ul class="nav navbar-nav">

                        <% if (page == '/') { %>
                          <li class="active"><a href="#">Home<span class="sr-only">(current)</span></a></li>
                        <% } else { %>
                          <li><a href="/">Home</a></li>
                        <% } %>



                        <% if (page == '/adoptableAnimals') { %>
                          <li class="active"><a href="#">Adoptable Animals<span class="sr-only">(current)</span></a></li>
                        <% } else { %>
                          <li><a href="/adoptableAnimals">Adoptable Animals</a></li>
                        <% } %>


                        <% if (page == '/adoptionApp') { %>
                          <li class="active"><a href="#">Adoption Application<span class="sr-only">(current)</span></a></li>
                        <% } else { %>
                          <li><a href="/adoptionApp">Adoption Application</a></li>
                        <% } %>


                        <% if (page == '/ourClients') { %>
                          <li class="active"><a href="#">Our Clients<span class="sr-only">(current)</span></a></li>
                        <% } else { %>
                          <li><a href="/ourClients">Our Clients</a></li>
                        <% } %>


                        <% if (page == '/donate') { %>
                          <li class="active"><a href="#">Donate<span class="sr-only">(current)</span></a></li>
                        <% } else { %>
                          <li><a href="/donate">Donate</a></li>
                        <% } %>


                        <% if (page == '/generalInfo') { %>
                          <li class="active"><a href="#">General Information<span class="sr-only">(current)</span></a></li>
                        <% } else { %>
                          <li><a href="/generalInfo">General Information</a></li>
                        <% } %>

                      </ul>

                      <ul class="nav navbar-nav navbar-right">
                        <li class="logout"><a href="/logout">LOGOUT</a></li>
                      </ul>
                    </div>
                  </div>
                </nav>
              </div>
            </div>
          </div>
        </div>





      <% } %>
