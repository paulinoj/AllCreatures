<!DOCTYPE html>
<html lang="en">
  <% include head %>
  <body>

   <div class="container">

   <% include navbar %>


  <div class="panel panel-primary">
    <div class="panel-heading">
      <div class="webPageHeading">Our Clients</div>
    </div>
  </div>

  <div class="main_section">
    <section id="photos">     
    </section>
  </div>


    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
    <script src="//netdna.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js"></script>
    <script>
      $(document).ready(function() {
      function getRandomSize(min, max) {
        return Math.round(Math.random() * (max - min) + min);
      }
        for (var i = 1; i < 24; i++) {
          // var rannum = getRandomSize(1, 24);
          var currentSrc = '"images/photo' + i + '.jpg"';
          $('#photos').append('<img src=' + currentSrc + '>');
        }
       for (var j = 1; j < 4; j++) {
          // var rannum = getRandomSize(1, 24);
          var currentSrc = '"images/photo' + j + '.png"';
          $('#photos').append('<img src=' + currentSrc + '>');
        }        
      });
    </script>
</body>
    </div>

  </div>

